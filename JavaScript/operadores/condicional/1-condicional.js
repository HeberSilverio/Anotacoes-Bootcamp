// Ternário
condicao ? valor1 : valor2

true ? 'foo' : 'bar'    // Retorna 'foo' // se a condição for verdadeira ele recebe o primeiro valor
false ? 'foo' : 'bar'    // Retorna 'bar' // se a condição for false ele recebe o segundo valor